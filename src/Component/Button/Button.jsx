import React from 'react';

const Button = (props) => {
    const {watchTime} = props.course

    const total = 0;



    for(const course of cart){
        total = total + course.cart;
    }
    return (
        <div>
            
        </div>
    );
};

export default Button;